{
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix admin-backend && npm install --prefix frontend && npm install --prefix admin-frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:admin-backend": "npm run dev --prefix admin-backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin-frontend": "npm run dev --prefix admin-frontend",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:admin-backend\" \"npm run dev:frontend\" \"npm run dev:admin-frontend\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.9",
    "tailwindcss": "^4.2.2"
  }
}
